/// <reference types="vite/client" />
/// <reference types="chrome" />

// `?script` and friends — CRXJS hands back the built filename, which the
// service worker feeds to chrome.scripting.executeScript().
/// <reference types="@crxjs/vite-plugin/client" />
